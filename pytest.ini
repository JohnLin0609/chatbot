[pytest]
asyncio_mode = auto
pythonpath = .
testpaths = tests
bdd_features_base_dir = features
addopts = -m "not integration"
markers =
    integration: tests that need a running redis + postgres (docker compose up -d)
